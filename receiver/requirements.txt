# Runtime dependencies, pinned.
#
# Exact versions so a deploy today and a rebuild in six months install the
# same tree. The cost is that security fixes do not arrive on their own —
# raise these deliberately and let CI verify the result.
#
# requirements.lock beside this file pins the transitive dependencies too, for
# a fully reproducible install:
#
#   pip install -r requirements.lock
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
