# Test-only dependencies, pinned. Not needed to run the receiver.
#
# starlette's TestClient needs an HTTP client of its own and does not declare
# one, so a clean environment fails at import with a RuntimeError rather than a
# missing-package error. It looks for httpx2 first and falls back to httpx.
pytest==9.1.1
httpx2==2.12.0
